<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="springcaching.modules.dao.ProviderDao">

    <select id="getProviders" resultType="springcaching.modules.entities.Provider">
        select * from provider
    </select>

    <select id="getProviderByPid" resultType="springcaching.modules.entities.Provider">
        select * from provider where pid=#{pid}
    </select>

    <insert id="addProvider">
        INSERT INTO provider
        ( provider_code,
          providerName,
          people,
          phone,
          address,
          fax,
          `describe`,
          create_date
        ) VALUES
        (
          #{providerCode},
          #{providerName},
          #{people},
          #{phone},
          #{address},
          #{fax},
          #{describe},
           now()
        )
    </insert>


    <update id="updateProvider" parameterType="springcaching.modules.entities.Provider">
        UPDATE provider SET
        provider_code=#{providerCode},
        providerName=#{providerName},
        people=#{people},
        phone=#{phone},
        address=#{address},
        fax=#{fax},
        `describe`=#{describe},
        create_date=#{createDate}
        WHERE pid=#{pid}
    </update>




    <delete id="deleteProviderByPid">
        delete from provider where pid = #{pid}
    </delete>
</mapper>